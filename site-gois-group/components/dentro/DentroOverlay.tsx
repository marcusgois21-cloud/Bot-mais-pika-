'use client'

/**
 * STUB — camada "Ver por dentro" (spec §15.3). Implementação completa: agente "sobre-dentro".
 * Colunas do grid, linhas de base, caixas tracejadas e chips para cada [data-dentro] e [data-dentro-vazio].
 */
export function DentroOverlay() {
  return null
}
